# Spring Boot 环境配置说明

- **PRD（产品需求文档）**：[docs/PRD-健身训练与饮食管理App.md](docs/PRD-健身训练与饮食管理App.md)
- **AI 餐食识图与热量估算（端到端开发流程）**：[docs/AI餐食识别与热量估算-开发流程.md](docs/AI餐食识别与热量估算-开发流程.md)

## 配置文件说明
- application.yml: 默认配置文件
- application-dev.yml: 开发环境配置 (数据库: gymappnew)
- application-prod.yml: 生产环境配置 (数据库: fitness_app)

## 运行方式
### 开发环境
java -jar target/fitness-backend-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev

### 生产环境  
java -jar target/fitness-backend-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod

### IDE开发
在VM options中添加: -Dspring.profiles.active=dev

### Maven打包
mvn clean package "-Dspring.profiles.active=prod"

### Futter打包
flutter pub get
flutter build web


**升级版一键部署脚本（企业规范版）**

✅ 应用统一放在：`/app`
✅ 环境统一放在：`/opt/tools`
✅ 数据统一放在：`/data`
✅ 日志统一放在：`/var/log/fitness`
✅ 支持后续扩展（多服务）

---

# 🚀 一键部署脚本（企业规范版）

## 使用方式

```bash
nano deploy.sh
```

粘贴👇

---

# 🔥 deploy.sh（完整版）

```bash
#!/bin/bash

set -e

echo "🚀 开始部署 Fitness App（规范版）..."

# =====================
# 统一目录结构
# =====================

APP_DIR="/app"
TOOLS_DIR="/opt/tools"
DATA_DIR="/data"
LOG_DIR="/var/log/fitness"

mkdir -p $APP_DIR
mkdir -p $TOOLS_DIR
mkdir -p $DATA_DIR/mysql
mkdir -p $DATA_DIR/redis
mkdir -p $LOG_DIR

echo "📁 目录结构创建完成"

# =====================
# 安装基础依赖
# =====================

apt update -y
apt install -y wget tar nginx

echo "📦 基础依赖安装完成"

# =====================
# 安装 Java（统一路径）
# =====================

cd $TOOLS_DIR

if [ ! -d "java" ]; then
    echo "⬇️ 安装 Java..."
    wget https://download.oracle.com/java/17/latest/jdk-17_linux-x64_bin.tar.gz -O java.tar.gz
    mkdir java
    tar -xzf java.tar.gz -C java --strip-components=1
fi

export JAVA_HOME=$TOOLS_DIR/java
export PATH=$JAVA_HOME/bin:$PATH

echo "☕ Java 安装完成"

# =====================
# 安装 MySQL（使用系统包）
# =====================

apt install -y mysql-server

systemctl start mysql
systemctl enable mysql

echo "🗄️ MySQL 安装完成"

# =====================
# 初始化数据库
# =====================

mysql -u root <<EOF
CREATE DATABASE IF NOT EXISTS fitness_app;
EOF

echo "📊 数据库初始化完成"

# =====================
# 安装 Redis
# =====================

apt install -y redis-server

systemctl start redis-server
systemctl enable redis-server

echo "⚡ Redis 安装完成"

# =====================
# 配置 Nginx
# =====================

cat > /etc/nginx/sites-available/fitness <<EOF
server {
    listen 80;

    server_name _;

    client_max_body_size 25m;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
    }
}
EOF

ln -sf /etc/nginx/sites-available/fitness /etc/nginx/sites-enabled/

systemctl restart nginx

echo "🌐 Nginx 配置完成"

# =====================
# 创建应用启动脚本
# =====================

cat > $APP_DIR/start.sh <<EOF
#!/bin/bash

export JAVA_HOME=$TOOLS_DIR/java
export PATH=\$JAVA_HOME/bin:\$PATH

cd $APP_DIR

echo "🚀 启动后端服务..."

nohup java -jar app.jar > $LOG_DIR/app.log 2>&1 &
EOF

chmod +x $APP_DIR/start.sh

# =====================
# 创建停止脚本
# =====================

cat > $APP_DIR/stop.sh <<EOF
#!/bin/bash

PID=\$(ps -ef | grep app.jar | grep -v grep | awk '{print \$2}')

if [ -n "\$PID" ]; then
    kill -9 \$PID
    echo "🛑 应用已停止"
else
    echo "⚠️ 未找到运行中的应用"
fi
EOF

chmod +x $APP_DIR/stop.sh

# =====================
# 创建重启脚本
# =====================

cat > $APP_DIR/restart.sh <<EOF
#!/bin/bash

$APP_DIR/stop.sh
sleep 2
$APP_DIR/start.sh
EOF

chmod +x $APP_DIR/restart.sh

# =====================
# 输出提示
# =====================

echo ""
echo "🎉 部署完成！"
echo ""
echo "👉 上传你的后端："
echo "scp target/app.jar root@服务器:$APP_DIR/"
echo ""
echo "👉 启动服务："
echo "cd $APP_DIR && ./start.sh"
echo ""
echo "👉 查看日志："
echo "tail -f $LOG_DIR/app.log"
echo ""
echo "👉 访问："
echo "http://服务器IP"
echo ""
```

---
# 👉 给 MySQL 设置一个真正的账号密码（生产必做）

第一步：进入 MySQL
sudo mysql
第二步：设置密码

执行：
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456';


# 🧠 目录结构说明（非常重要）

部署完成后你的服务器结构会变成：

```bash
/app
  ├── app.jar
  ├── start.sh
  ├── stop.sh
  ├── restart.sh

/opt/tools
  └── java

/data
  ├── mysql
  └── redis

/var/log/fitness
  └── app.log
```

👉 这是**标准后端服务目录结构**

---

# 🚀 使用流程（一步步）

## 1 执行脚本

```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 2 上传后端

```bash
scp target/app.jar root@服务器:/app/
```

---

## 3 启动

```bash
cd /app
./start.sh
```

---

## 4 查看日志

```bash
tail -f /var/log/fitness/app.log
```

## start.sh  一定要指定--spring.profiles.active=prod
#!/bin/bash

export JAVA_HOME=/opt/tools/java
export PATH=$JAVA_HOME/bin:$PATH

cd /app/fitness

echo "🚀 启动后端服务..."

nohup java -jar app.jar --spring.profiles.active=prod > /app/fitness/app.log 2>&1 &

访问测地址：
http://101.43.58.137:8080/swagger-ui/index.html
http://101.43.58.137:8080/v3/api-docs

# 前端打包部署
打包前将前端请求后端的url调整
方案 A：前后端同域名部署，前端请求相对路径
最推荐。

思路是：
Flutter Web 部署到 Nginx 的 80
Nginx 把 /api/、/swagger-ui/、/v3/api-docs/ 反代到 Spring Boot 127.0.0.1:8080
Flutter 前端里的 baseUrl 改成空域名方案，比如：
''
或 Uri.base.origin
或直接拼相对路径
这样页面和接口都走同一个域名/IP，不会有跨域问题。

方案 B：前端直接请求公网后端地址
例如改成：

http://101.43.58.137
这种也能跑，但不如方案 A 干净，而且以后上 HTTPS、换域名都麻烦。

## 标准打包命令就是：

cd frontend
flutter pub get
flutter build web

flutter pub get
flutter build web --dart-define=API_BASE=https://你的域名或IP

flutter build web --dart-define=API_BASE=http://101.43.58.137
打包成功后，产物通常在： 上传至服务器

frontend/build/web

## 安装 Nginx
apt install nginx -y
## 配置 Nginx

nano /etc/nginx/sites-available/fitness

server {
    listen 80;
    server_name 101.43.58.137;
    client_max_body_size 25m;

    root /app/fitness/fitness-web;
    index index.html;

    # AI 识餐等接口为 JSON + Base64，默认 1m 会返回 413
    client_max_body_size 25m;

    access_log /var/log/nginx/fitness_access.log;
    error_log /var/log/nginx/fitness_error.log;

    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /swagger-ui/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /v3/api-docs/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

## 校验 Nginx 配置：
nginx -t
sudo systemctl reload nginx

##  启用配置
ln -s /etc/nginx/sites-available/fitness /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

## 浏览器访问：
http://101.43.58.137/
http://101.43.58.137/swagger-ui/index.html

## 常见问题：AI 识餐返回 413（nginx/1.x）

现象：弹窗里出现 `413 Request Entity Too Large`。原因是 **Nginx 默认只允许约 1MB 请求体**，而照片 Base64 会大很多。

**处理：** 在 `server { ... }` 里增加（或调大）如下，保存后 `nginx -t` 再 `systemctl reload nginx`：

```nginx
client_max_body_size 25m;
```

同时保证 Spring Boot 已允许较大 POST（本仓库 `application.yml` / `application-prod.yml` 中已配置 `server.tomcat.max-http-form-post-size: 25MB`），修改后端配置后需**重启 jar**。