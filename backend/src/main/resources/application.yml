spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gymappnew?useSSL=false&serverTimezone=UTC
    username: root
    password: fcl950801
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
  # AI 识餐等为 JSON 大包（Base64 图）；与 Nginx client_max_body_size 一并放宽
  tomcat:
    max-http-form-post-size: 25MB

jwt:
  secret: "change-me-to-a-secure-secret-change-me-123456"
  expiration-seconds: 604800

ai:
  ark:
    api-key: ${FOOD_AI_API_KEY:df868073-2f9e-4f83-a821-48d6cbadb31f}
    base-url: ${FOOD_AI_BASE_URL:https://ark.cn-beijing.volces.com/api/v3}
    model: ${FOOD_AI_MODEL:ep-20260320115953-9p99f}

